# Dependencies
node_modules

# Build output
.astro
dist

# OS / editor junk
.DS_Store
=4

# Claude / tooling local state
.claude/worktrees/
.claude/settings.local.json
.gstack/

# Local launchers
start-website.command/

# One-off backups
.hero_ai_backup_*/

# One-off reports / audits (Phase 1.x scratch output)
*_AUDIT_*.md
*_REPORT_*.md
AUTHORITY_*.csv
BACKFILL_*
ENRICH*
EXPANSION_*
GENERATION_*
FAQ_OPTIMIZATION_*
DUPLICATES_TO_DELETE.txt
GENERATED_PAGES.txt

# Keep (explicitly DO commit):
#   AUTHORITY_DELTA_*.md
#   GEO_CITATION_BASELINE_*.md

# === Phase 2 cleanup additions ===

# Finder dupes (space-2 pattern)
* 2.jpg
* 2.png
* 2.webp
* 2.json
* 2.ts

# Scratch / agent handoff files
_agent*.txt
_cp_*.txt
_faq_agent*.txt
_link_*.txt
_link_*.json
_routes_master.json
__pycache__/
*.bak

# Old scratch script versions (keep on disk, just don't track)
download_images.py
download_images_v[0-9].py
expand_summaries.py
expand_summaries_v[0-9].py
expand_final.py
faq_optimizer.py
faq_optimizer_production.py
faq_rewriter.py
faq_rewriter_v[0-9].py
faq_rewriter_final.py
rewrite_faqs.py
rewrite_faqs_v[0-9].py
enrich-batch*.cjs
fix-remaining.cjs
inject-nav.cjs

# Raw customer data (not part of website)
全球*.xlsx

# One-off test/bundle artifacts
_test-nav.mjs
_bundled-render.mjs

# === Apr 2026 hero-audit + pillar-build scratch ===

# Build cache / check-output dirs
.astro-new-cache/
dist-pillar-check/

# Audit output
_audit-out/
_route-audit*.mjs
image-audit.json
image-duplicates.json
dedupe-report.md

# Old upgrade scratch (Apr 14 static HTML dump)
_upgraded/

# Third-party / unrelated clones in workspace
awesome-design/
testdir/

# Obsolete bash helpers
cleanup-and-commit.sh
commit-nav-restructure.sh
generate-static-html.js
_test_delete_me.txt

# Raw image originals (backups of replaced heroes — keep locally only)
public/landing-images/_originals/


# debug build snapshots
dist-w*/
_audit-out/

# One-off scratch scripts / validators (April 2026 session)
_deadlink.mjs
validate_resources.mjs
tmp_emdash_reduce.py
tmp_whether_sentences.csv

# Reports / audits / batch summaries — kept on disk for working reference
# but not tracked. Matches the same intent as the root-level *_AUDIT_*.md /
# *_REPORT_*.md / AUTHORITY_*.csv ignore patterns above.
reports/
.lighthouseci/

# Phase 6 #4 image-compression backup (originals preserved locally before
# in-place re-encode of oversized hero JPEGs). Keep on disk for visual
# regression checks; not source-of-truth.
.hero_pre_phase6_backup_2026-04-27/

# Stage 3 cutover diff tool output — twin dist trees built by
# scripts/stage3-diff.mjs. Regenerated on every script invocation; never
# part of source control.
dist-baseline/
dist-experimental/

# Temp cleanup scripts + commit messages (used during refactor work)
.cleanup-*.sh
.commit-msg.txt
