fix(seo): dedupe sitemap — park shadowed editorial jsons, drop double source

sitemap.xml double-listed 5 URLs (audit warning SITEMAP_DUPLICATE_URLS,
frozen as knownWarnings since the baseline was first captured). Two
distinct root causes:

1. Four routes had TWO editorial json files each declaring the same route
   after slug renames / content consolidations left the losing copy
   behind (buildEditorialPages pushed both; EDITORIAL_ROUTE_INDEX is
   last-wins so the live render never changed). Parked the non-rendering
   copies under src/content/editorial/_unused/ per repo convention:
   - about/case-studies.json (superseded by case-studies/_pillar.json)
   - compare/mifare-plus-ev2-vs-desfire-ev3.json (live content lives in
     compare/mifare-plus-vs-desfire.json)
   - compare/pps-vs-silicone-laundry-tags.json
   - compare/rfid-hotel-card-vs-magnetic-stripe.json
   - lp/rfid-tag-factory.json (live content lives in
     lp/uhf-rfid-tag-manufacturer.json)

2. /case-studies/ was listed both by siteData.pages (editorial _pillar
   synthetic) and by the native-hub sitemap supplement. Flipped its
   supplementSitemap to false; test expectation updated with rationale.

No rendered page changes — every URL keeps the exact content it had.
site-contract baseline re-frozen: SITEMAP_DUPLICATE_URLS known-warning is
now zero and the audit stays as the tripwire for future duplicates.

Verified: 322 tests, lint/check/chip gates, triple build, site contract
audit warnings=0 sitemap=520/520, canary audit PASS (533 markers / 549
hubs), image-sitemap equal across builds.
