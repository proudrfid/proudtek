blog-voice rollout HALTED 2026-05-30 — user decision (收工 / declared done at the tail).

State at halt:
  • ~46 posts enlivened (merged)
  • ~57 blog-voice PRs in flight — these remain valid and merge normally
  • ~9–10 unclaimed Tier A posts remaining, ALL carrying {chip:} placeholders
    (1–101 placeholder lines each). Per-post review cost now exceeds the value
    of a single centralized cold-open — the low-ROI tail.

Do NOT open another blog-voice PR while this file exists.

To resume:
  • delete STOP_BLOG_VOICE at repo root (or run the dispatcher with --force), AND
  • prefer batching the placeholder-heavy tail into ≥5-file PRs rather than the
    1-post-per-PR cadence, or run the remaining Tier B / sensitive posts under
    explicit human review.
