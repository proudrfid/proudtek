chore(editorial): Stage 3 cleanup — drop legacy render path + parity tests [P3-cleanup]

Post-cutover cleanup (option A+ per cutover memo). Shadow component tree
is now the SOLE render path for editorial pages — the env-flag dispatch,
the legacy parity test suite, and the verification tooling all go away.

Cuts:
- src/pages/[...slug].astro: removed STAGE3_ROUTE_PREFIXES + env-flag
  check + progressive-rollout doc block. Dispatch is now a one-liner
  branching on page.editorialDefinition.
- src/lib/editorial-pages.ts buildBodyHtml: no longer calls
  renderEditorialMain — just empties <main> so EditorialPageLayout's
  regex-split + <EditorialArticle> fills it. renderEditorialMain plus
  the ~24 render* sub-renderers remain in the file as dead code
  unreachable from production; follow-up pass will surgically delete
  them (~1500 lines).
- Deleted 22 parity tests + _parity-helpers.ts. Legacy render path is
  gone, so byte-equivalence comparison has nothing to compare against.
- Deleted src/pages/_stage3/lp-experiment.astro (Stage 3 quarantine).
- Deleted scripts/stage3-diff.mjs + stage3-debug-single.mjs.
- Removed stage3:diff npm script from package.json.

Rewrites:
- 3 snapshot tests (editorial-pages-leaf / -variants / -integration)
  rewritten to render shadow components via Astro Container API.
  Old .snap files deleted; vitest regenerates them on first run.
  These now lock SHADOW output as the source of truth — long-term
  drift detector for the EditorialArticle.astro + sub-component tree.
- _smoke.test.ts: inlined normalizeHtml (was imported from the now-
  deleted _parity-helpers.ts).

Docs:
- docs/architecture/editorial-stage-3-cutover-memo.md: added cleanup
  pass section + surviving test surface table + follow-up tasks.
- docs/architecture/editorial-rendering-debt.md: marked Stage 3 SHIPPED;
  noted that editorial-pages.ts dead-code removal remains as follow-up.

Test surface after cleanup:
  - editorial-types.test.ts (39 schema tests)
  - _smoke.test.ts (1 Container API canary)
  - editorial-pages-leaf.snapshot.test.ts (8 shadow leaf snapshots)
  - editorial-pages-variants.snapshot.test.ts (~25 shadow variant snapshots)
  - editorial-pages-integration.snapshot.test.ts (5 shadow page snapshots)
  Total: ~78 assertions.

Rollback: git revert this commit restores the env-flag dispatch + 22
parity tests. The shadow tree itself (37 components + EditorialPageLayout
+ Sprint 1 retrofits) stays committed; cutover stays in place.
