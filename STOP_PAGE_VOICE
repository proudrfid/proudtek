page-voice STANDARD autopilot rollout declared done — 2026-06-01.

WHY THIS FILE EXISTS
The dispatcher tripped FEW_FILES_REMAIN: only 2 unclaimed RICH·STANDARD
pages remained, both being raced by the active swarm, against 71 claims
already in flight (open PRs / branches / WIP) and 8 merged. At that point
the per-page review cost of the 1-PR-per-page cadence exceeds the value of
chasing the last scraps. Halting the whole swarm here.

WHAT IS *NOT* DONE (do NOT autopilot these)
- 4 SENSITIVE pages (medical / patient-safety / pharma) still need voice
  work. These are excluded from the dispatcher by design — run them as a
  deliberate, human-reviewed batch with humor capped ≤ 2/10, never wit on
  a harm outcome. Not via `npm run page-voice:next`.
- Tail scraps still unmerged: nfc-business-card-programs,
  nfc-business-card-iphone-android-compatibility, plus the large open-PR
  backlog. Let the in-flight PRs drain, then batch any stragglers into a
  single ≥5-file PR rather than reopening 1-per-PR autopilot.

TO RESUME
Delete this file (or pass `--force` to the dispatcher). Removing it
re-opens the swarm — only do that once you intend a fresh batch.
